# NOTE: Derived from blib/lib/Inline.pm.
# Changes made here will be lost when autosplit is run again.
# See AutoSplit.pm.
package Inline;

#line 1552 "blib/lib/Inline.pm (autosplit into blib/lib/auto/Inline/M16_DIRECTORY_mkdir_failed.al)"
sub M16_DIRECTORY_mkdir_failed {
    my ($dir) = @_;
    return <<END;
Can't mkdir $dir to build Inline code.

END
#'
}

# end of Inline::M16_DIRECTORY_mkdir_failed
1;
